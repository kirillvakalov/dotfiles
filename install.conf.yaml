- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.gitignore.global:
    ~/.hushlogin:
    ~/.zprofile:
      force: true
    ~/.config/starship.toml: starship.toml

- shell:
  - [git submodule update --init --recursive, Installing submodules]
