git:
  overrideGpg: true
  pagers:
    - externalDiffCommand: difft --color=always --syntax-highlight=off
    - pager: delta --light --paging=never
gui:
  authorColors:
    "*": "blue"
  theme:
    # Config below is generated by scripts/theme.sh.
    # 💡 Please run this script again after you change ghostty theme to update
    # colors to new values
    selectedLineBgColor:
      - '#cccccc'
